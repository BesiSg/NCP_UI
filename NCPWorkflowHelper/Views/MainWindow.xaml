<mah:MetroWindow 
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" 
        x:Class="NCPWorkflowHelper.Views.MainWindow"
        xmlns:prism="http://prismlibrary.com/"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
         prism:ViewModelLocator.AutoWireViewModel="True" xmlns:it="http://schemas.microsoft.com/xaml/behaviors"
        Title="MainWindow" Height="1080" Width="1920">
    <it:Interaction.Triggers>
        <it:EventTrigger EventName="Closing">
            <it:InvokeCommandAction Command="{Binding OnClosingCommand}" />
        </it:EventTrigger>
    </it:Interaction.Triggers>
    <Grid>
        <Button Height="30" Width="30" Margin="10" Style="{StaticResource Button_FrameMenu}" ToolTip="Save" Command="{Binding SaveDataCommand}" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Image Source="/Resource/Save_Icon.png" Style="{StaticResource ImgBtn_W25H25}"/>
        </Button>
        <StackPanel Orientation="Horizontal" Margin="0,10,10,0" >
            <TabControl Margin="10">
                <TabItem Header="User account" Style="{StaticResource MahApps.Styles.Control.WindowCommands}">
                    <ContentControl prism:RegionManager.RegionName="UserAccountRegion" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="5"/>
                </TabItem>
                <TabItem Header="Fetch branch" Style="{StaticResource MahApps.Styles.Control.WindowCommands}">
                    <ContentControl prism:RegionManager.RegionName="FetchBranchRegion" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="5"/>
                </TabItem>
                <TabItem Header="Patch Viewer" Style="{StaticResource MahApps.Styles.Control.WindowCommands}">
                    <ContentControl prism:RegionManager.RegionName="PatchViewerRegion" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="5"/>
                </TabItem>
                <TabItem Header="Commit branch" Style="{StaticResource MahApps.Styles.Control.WindowCommands}">
                    <ContentControl prism:RegionManager.RegionName="CommitBranchRegion" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="5"/>
                </TabItem>
            </TabControl>
        </StackPanel>
    </Grid>

    <!--<TabControl HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10,0,10,10">
            <TabItem Header="Filters">
                <FormUI:FilterList x:Name="FilterList" Margin="0" MouseLeave="FilterList_MouseLeave"/>
            </TabItem>
            <TabItem Header="Issue Form">
                <FormUI:TicketInfo x:Name="IssueForm" Margin="0"/>
            </TabItem>
        </TabControl>-->
</mah:MetroWindow>
